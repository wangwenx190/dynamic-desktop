class DDIPC
{
    SIGNAL(clientMessage(QPair<QString, QVariant> message))
    SIGNAL(serverMessage(QPair<QString, QVariant> message))
    SLOT(void sendMessageToServer(const QPair<QString, QVariant> &message))
}
